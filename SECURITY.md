# 安全与数据边界

眼只通过本机 WxLens HTTP 接口读取聊天索引，从明确账号的允许目录读取已下载附件；不自行读微信数据库、提取密钥、注入进程、修改消息或操作联系人。

## 数据去向

- 微信读取接口只接受 `http://127.0.0.1` 或 `http://[::1]`，拒绝远端主机、用户信息、附加路径和重定向。
- AI调用获得的消息和附件正文会进入该AI客户端及其配置的模型处理流程。眼的本机读取不意味着所接AI必然离线。
- `.local/config.json` 保存本机设置；`.local/watchlists/` 保存规则、检查点、续扫进度和未确认批次；`.local/receipts/` 保存CLI结果；`output/` 保存证据包。上述目录为明文私人数据。
- 基础npm安装和用户主动安装Python依赖、下载模型会联网。聊天查询不自动抓取文章网页或下载缺失附件、模型。查询可按设置后台启动已安装的WxLens；首次安装器只在用户运行setup并交互确认后执行。

## 保护措施及限制

附件读取与导出使用规范化真实路径检查，测试覆盖Windows目录联接。输出文件使用独立名称及排他创建，避免覆盖历史结果。单次路径检查不能抵御拥有同等本机权限的进程在检查与读取之间恶意替换路径；不要给不可信程序同等目录写权限。

压缩包在交给Office/ZIP解析器前执行条目元数据和实际展开流字节检查。HTTP限制超时和响应大小，MCP使用本地stdio，不额外开放网页端口。本机同用户恶意进程不属于MCP能独立隔离的边界。

关注列表采用排他文件锁与原子替换。待确认结果不会被下一轮覆盖；超过状态字节预算时写入失败，原检查点保持。异常进程退出可能留下锁文件，恢复步骤见 `docs/monitoring.md`。

导出自动脱敏只覆盖部分结构化路径和微信ID，不覆盖所有自由文本、名字、手机号、地址或秘密。它是本地证据包，分享前逐项检查；校验和证明文件未变化，不证明来源、结论或脱敏完整。

## 报告问题

请先提供脱敏的复现步骤、版本、错误类型和合成样例。不要把真实聊天、`.local`、账号目录、数据库、凭据或未检查的导出ZIP提交到公开Issue。若GitHub仓库已启用私密漏洞报告，可使用该入口；否则先在Issue说明存在需要私下处理的问题，不公开敏感细节。
