# 变更记录

## 4.1.8 — 2026-09-25

- 启动时优先使用原程序 `WxLens.exe`，避免改名后的别名改变 Electron 的本地数据和登录环境。
- 首次设置会在原程序旁创建非破坏性的 `yan.exe` 硬链接；原程序、卸载程序和已有数据均不改动。
- 已保存的 `yanExe` 即使指向 `yan.exe` 硬链接，也会稳定回到原程序路径；独立的 `yan.exe` 不会被误替换。

## 4.1.7 — 2026-09-25

- 产品名称统一为「眼」。安装包中的安装器文件名为 `vendor/yan-4.3.0-Setup.exe`，文件内容与原来的安装器相同，SHA-256 不变。
- 新的配置项是 `yanExe` 和 `--yan-exe`，环境变量使用 `YAN_*`。已经保存的 `wxlensExe` 和 `WXLENS_*` 仍可读取，避免已有安装失效。
- 眼仍会在原来的常见安装位置查找本机程序，所以已经安装好的程序不需要重装。

## 4.1.6 — 2026-09-25

- 统一消息完整性契约：识别本机索引的 `truncated`、`contentTruncated`、`rawContentTruncated`、`originalLength` 不一致和 zstd 未解码状态；精确回查会将这些情况标为 `partial`，分页结果和监控批次保留 `contentComplete` 与完整性统计。
- 搜索、合并转发和聊天内文章读取不再按字符串长度挑选正文；完整可读的搜索索引正文优先于被本机索引截断的压缩字段，精确回查负责回填发送者、类型和稳定标识。
- `text_only` 现在显式标记 `contentSuppressed`、原正文长度和替换原因；`[多媒体]` 仅是输出视图，不再被误认为媒体正文已经读取。
- 关注列表身份键优先使用会话内稳定 `localId`/`serverId`，压缩正文解码前后不再因正文表示变化产生新身份；旧状态首次升级可能需要观察一次重叠批次。
- 增加 `npm run optional:local` 本地模式：只探测并启用工作区或本机已有的 xlrd、RapidOCR/PyMuPDF、faster-whisper 解释器，不运行 pip、不联网、不下载模型；`doctor` 按能力报告 optional/pass。
- 损坏的 zstd 帧在 Node 22 上可能解出空缓冲，在当前 Node 24 上会解压失败。两种结果都保留原字段并标记未解码，不再把压缩十六进制当成明文。合法空帧仍标记为 `zstd-empty`。

## 4.1.5 — 2026-09-24

- 以 `28b52ffd` 开头的 zstd 十六进制正文会先解成 UTF-8 再进入列表、精确回查、上下文和关注批次。解压失败、解出空内容、不是 UTF-8 或超过 8MiB 时保留原字段并标记 `contentUndecoded`，不再把压缩十六进制说成未截断的明文。
- 关键词搜索命中如果缺少发送者或消息类型，会在同一会话的有界扫描里按 `localId` 回填。发送者筛选和 `text_only` 使用回填后的字段。已解压的完整正文优先于搜索索引文本。
- 附件正文搜索不再把正文关键词当成文件名过滤。文件名不含该词、正文含该词时仍会命中。按文件名筛选仍只属于 `list_wechat_attachments`。
- 图片、表情和文件消息在本机索引没有给出本地路径时仍只保留占位符。眼不解密 `.dat`，也不按修改时间猜测文件归属。

## 4.1.4 — 2026-09-24

- 聊天消息不再由眼侧按 2000 字符截断。关注批次保留本机索引返回的完整 `content`，并给出 `contentChars` 与 `contentBytes`；只有上游明确报告截断时，`contentTruncated` 才为 true。
- 新增 `get_message_by_id`：按会话和 `localId` 有界回查单条正文，未命中时返回 `nextOffset`，不把当前窗口未找到说成历史中不存在。
- 合并转发和聊天内文章卡片优先回查原消息；分析 JSON 保留原始正文，Markdown 中的 300 字摘录和条数上限只是展示，并显式标记。
- 附件正文支持 `offset_chars` / `nextOffset` 分块读取，分块不会拆开 Emoji。附件搜索对每个文件一次提取最多 100 万字符，不再按 20 万字符切开，因此预算内横跨旧分块边界的关键词不会被漏掉。仍有尾部时返回 `partial` 和 `truncatedFiles`。
- 证据 ZIP 的附件清单写入 `returnedChars`、`offsetChars` 和 `nextOffset`。未指定 `max_chars_per_attachment` 时导出最多 100 万字符，而不是只留默认前 20 万字符。
- 公众号解析不再把正文切到 20 万字符，也不再丢弃第 100 张之后的配图。读取和下载配图的序号上限同步放宽；每次下载仍最多 20 张。HTML 超过 4MiB 或存档超过 8MiB 时整份失败。
- 单次本机 HTTP 响应仍限制 16MiB，关注批次持久化仍限制 16MiB；超限直接失败，不保存半截正文。

## 4.1.3 — 2026-09-16

- 新增公众号候选账本：搜索结果自动去重入库，验证码中断后保留 remaining，可按状态查询。
- 原文抓取/导入可绑定 candidate_id；账号名一致为 verified，不一致为 conflicting，已核验状态不会被再次搜索降级。冲突核验不会覆盖候选原账号名，避免第二次核验被误标为通过。
- 文章增加 identityStatus：URL 含 biz 为 verified，仅名称或脚本 biz 为 candidate。

## 4.1.2 — 2026-09-16

- 修复公众号脚本元数据误匹配：不再把 `sct`/`act`/`expect`/`sbiz` 或对象属性当成 `ct`/`biz`；仅静态解析顶层字面量，并校验时间戳与 biz 格式。
- 非法日历日期不再被 JavaScript 归一成另一天。
- 搜狗批量搜索拒绝无效 `days`；请求间隔 1.2 秒，最多 12 次/45 秒；验证码、429、403 立即停止并返回未完成范围。
- 截断 GIF/PNG/WebP/JPEG 不再被当作完整图片返回。
- Windows 图片打开竞态回归改为真实路径拦截，不再跳过关键断言。

## 4.1.1 — 2026-09-14

- 兼容 Windows/Linux 的图片路径安全回归测试修正；GitHub Actions 全矩阵通过。
- 保持 4.1.0 的公众号多轮发现、文章读取、配图和质量边界能力。

## 4.1.0 — 2026-09-14

- 新增公众号文章读取、浏览器快照导入、文章配图下载与原生 MCP 图片返回。
- 新增聊天分享文章提取、已收集文章历史查询，以及搜狗公开索引和可选腾讯云 WSA SearchPro 候选发现。
- 本地图片读取增加打开文件身份核对；文章存档增加完整性校验、原子保存和重复快照冲突记录。
- 搜索结果只作为候选发现，不宣称公众号完整历史；腾讯云入口默认关闭且需要用户自行开通和配置凭据。

## 4.0.1 — 2026-09-14

- 真实验收发现眼的健康接口返回`status: ok`，修复诊断误报与启动等待误判；其他业务接口保持严格的`ok/data`封装检查。
- 增加对应回归，25项测试通过。真实Codex CLI完成列2群、读1条消息；私人原文不进入公开证据。
- 新增面向AI的接入与分层验收指南，说明旧客户端配置、自定义眼的安装路径和模型认证边界。

## 4.0.0 — 2026-09-14

### 面向AI的MCP能力

- 显示名称统一为「眼」，MCP服务名为`yan`。
- 26个工具、4个工作流提示词、`yan://guide`资源；无resource支持的客户端可调用指南工具。
- 指定发送者精确筛选、跨会话人物提取、多群关键词any/all与独立offset续读。
- 日报、周报、人物、项目、决定待办、风险争议、资源等总结证据包；统计与来源分群保留。
- 持久关注列表、未确认批次重试、连续交付分页、幂等ack、跨轮积压追赶、同秒边界扫描、并发锁和写前大小预算。
- 显式CLI轮询，可有限轮或持续运行；先保存结果文件再确认。

### 可验证的修复

- 分页游标仅推进已消费记录，避免页中limit导致漏读；重复页有界停止。
- 增量未读完整时保留确认游标，不提前越过积压。
- 附件和输出真实路径检查，阻挡目录联接越界。
- 本机HTTP地址限制、拒绝重定向、可取消超时和响应字节预算。
- Office/ZIP解析前执行实际展开流限额。
- 自动附件匹配只给候选；导出正文需显式选择，避免其他会话文件被带入。
- 导出隐私声明反映有限脱敏，README纳入SHA-256清单，ZIP重新读取验证。
- OCR模型显式本地路径，语音模型本地缓存读取；混合PDF逐页处理，缺OCR不丢弃已有语音结果。

### 接入与发行

- MCP握手不再等待外部本机服务，也不强制附件目录或Electron启动器。
- Node.js22+基础安装；可选Python与媒体运行时拆分。
- MCP直接使用、生成JSON/Proma/Codex TOML配置；恢复眼的自动发现和后台启动、首次设置与原安装器校验衔接。
- 真实stdio集成测试、边界回归、Windows/Linux CI、源码白名单打包与哈希复验。
- 完整MIT、第三方说明、Node依赖清单、公开源码隐私排除规则。

### 兼容变化

- `search_messages`返回带`messages`、`returned`和`boundary`的对象，以明确上游50条命中限制；旧调用方如只接受数组需调整。
- 导出不再默认提取启发式附件；`auto_link_attachments`只列候选。
- 源码版不含安装器；整合包可保留原安装器，经setup交互确认后执行。基础安装不强制OCR或Electron的Node模式。
- 关注功能对上游顺序、索引延迟、回填和资源上限有明确边界，见`docs/monitoring.md`。
