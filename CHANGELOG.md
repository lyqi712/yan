# 变更记录

## 4.1.3 — 2026-09-16

- 新增公众号候选账本：搜索结果自动去重入库，验证码中断后保留 remaining，可按状态查询。
- 原文抓取/导入可绑定 candidate_id；账号名一致为 verified，不一致为 conflicting，已核验状态不会被再次搜索降级。冲突核验不会覆盖候选原账号名，避免第二次核验被误标为通过。
- 文章增加 identityStatus：URL 含 biz 为 verified，仅名称或脚本 biz 为 candidate。

## 4.1.2 — 2026-09-16

- 修复公众号脚本元数据误匹配：不再把 `sct`/`act`/`expect`/`sbiz` 或对象属性当成 `ct`/`biz`；仅静态解析顶层字面量，并校验时间戳与 biz 格式。
- 非法日历日期不再被 JavaScript 归一成另一天。
- 搜狗批量搜索拒绝无效 `days`；请求间隔 1.2 秒，最多 12 次/45 秒；验证码、429、403 立即停止并返回未完成范围。
- 截断 GIF/PNG/WebP/JPEG 不再被当作完整图片返回。
- Windows 图片打开竞态回归改为真实路径拦截，不再跳过关键断言。

## 4.1.1 — 2026-09-14

- 兼容 Windows/Linux 的图片路径安全回归测试修正；GitHub Actions 全矩阵通过。
- 保持 4.1.0 的公众号多轮发现、文章读取、配图和质量边界能力。

## 4.1.0 — 2026-09-14

- 新增公众号文章读取、浏览器快照导入、文章配图下载与原生 MCP 图片返回。
- 新增聊天分享文章提取、已收集文章历史查询，以及搜狗公开索引和可选腾讯云 WSA SearchPro 候选发现。
- 本地图片读取增加打开文件身份核对；文章存档增加完整性校验、原子保存和重复快照冲突记录。
- 搜索结果只作为候选发现，不宣称公众号完整历史；腾讯云入口默认关闭且需要用户自行开通和配置凭据。

## 4.0.1 — 2026-09-14

- 真实验收发现WxLens健康接口返回`status: ok`，修复诊断误报与启动等待误判；其他业务接口保持严格的`ok/data`封装检查。
- 增加对应回归，25项测试通过。真实Codex CLI完成列2群、读1条消息；私人原文不进入公开证据。
- 新增面向AI的接入与分层验收指南，说明旧客户端配置、自定义WxLens安装路径和模型认证边界。

## 4.0.0 — 2026-09-14

### 面向AI的MCP能力

- 显示名称统一为「眼」，MCP服务名为`yan`，保留原有`WXLENS_*`配置和基础工具名。
- 26个工具、4个工作流提示词、`yan://guide`资源；无resource支持的客户端可调用指南工具。
- 指定发送者精确筛选、跨会话人物提取、多群关键词any/all与独立offset续读。
- 日报、周报、人物、项目、决定待办、风险争议、资源等总结证据包；统计与来源分群保留。
- 持久关注列表、未确认批次重试、连续交付分页、幂等ack、跨轮积压追赶、同秒边界扫描、并发锁和写前大小预算。
- 显式CLI轮询，可有限轮或持续运行；先保存结果文件再确认。

### 可验证的修复

- 分页游标仅推进已消费记录，避免页中limit导致漏读；重复页有界停止。
- 增量未读完整时保留确认游标，不提前越过积压。
- 附件和输出真实路径检查，阻挡目录联接越界。
- 本机HTTP地址限制、拒绝重定向、可取消超时和响应字节预算。
- Office/ZIP解析前执行实际展开流限额。
- 自动附件匹配只给候选；导出正文需显式选择，避免其他会话文件被带入。
- 导出隐私声明反映有限脱敏，README纳入SHA-256清单，ZIP重新读取验证。
- OCR模型显式本地路径，语音模型本地缓存读取；混合PDF逐页处理，缺OCR不丢弃已有语音结果。

### 接入与发行

- MCP握手不再等待外部WxLens，也不强制附件目录或Electron启动器。
- Node.js22+基础安装；可选Python与媒体运行时拆分。
- MCP直接使用、生成JSON/Proma/Codex TOML配置；恢复WxLens自动发现和后台启动、首次设置与原安装器校验衔接。
- 真实stdio集成测试、边界回归、Windows/Linux CI、源码白名单打包与哈希复验。
- 完整MIT、第三方说明、Node依赖清单、公开源码隐私排除规则。

### 兼容变化

- `search_messages`返回带`messages`、`returned`和`boundary`的对象，以明确上游50条命中限制；旧调用方如只接受数组需调整。
- 导出不再默认提取启发式附件；`auto_link_attachments`只列候选。
- 源码版不含安装器；整合包可保留原WxLens安装器，经setup交互确认后执行。基础安装不强制OCR或Electron的Node模式。
- 关注功能对上游顺序、索引延迟、回填和资源上限有明确边界，见`docs/monitoring.md`。
